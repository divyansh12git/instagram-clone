import 'package:flutter/material.dart';



class AppScreenLayout extends StatelessWidget {
  const AppScreenLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Center(child: Text("This is App"),)
    );

  }
}